# Content Rules

- Content lives in files first, not in code.
- Frontmatter should stay simple and readable.
- Slugs should be explicit and stable.
- Excerpts should be useful in metadata and listings, not filler.
- Add new fields only when multiple real entries need them.
- Keep bodies editable by humans without a custom toolchain.

